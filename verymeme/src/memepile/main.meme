// fn readf(data: *u8, size: i32) i32 {}

fn main() i32 {
  var SIZE : const i32 = 128;
  var data : [128]u8;
  var nbytes : i32 = readf(&data[0], SIZE);
  printf("got %d bytes: '%s'\n", nbytes, &data[0]);
  return 0;
}

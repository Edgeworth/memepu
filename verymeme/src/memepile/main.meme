
fn main() i32 {
  SIZE := 128u;
  data : [SIZE]u8 = {};
  nbytes := readf(&data[0], SIZE);
  printf("got %d bytes: ", nbytes);
  print_cstr(&data[0]);
  printf("\n");
  printf("strlen result: %d\n", strlen(&data[0]));

  str := string.new(&data[0]);
  print_str(&str);
  ret 0;
}

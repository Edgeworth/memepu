enum Result<T, E> {
  Ok(T),
  Error(E),
}

func trysomething(i8 succeed) Result<i8, bool> {
  if succeed > 3 {
    return Ok(5);
  } else {
    return Err(false);
  }
  return Ok(1);
}

func main() i8 {
  match trysomething(4) {
    Ok(v) {
      print("Got value");
    } Err(e) {
      print("Got error");
    }
  }
}

enum Unit {}
struct vector<T> {}

fn bubblesort<T>(v: const* vector<*T>) Unit {
  for i := 0; i < v.size(); i++ {
    for j := 0; j < v.size() - 1; j++ {
      if v[j] > v[j + 1] {
        swap(v[j], v[j + 1]);
      }
    }
  }
}

enum Unit {}

fn bubblesort<T>(vector<T*>* v) Unit {
  for (i32 i = 0; i < v.size(); i++) {
    for (i32 j = 0; j < v.size() - 1; j++) {
      if v[j] > v[j + 1] {
        swap(v[j], v[j + 1]);
      }
    }
  }
}

fn main() i32 {
  auto v = vector<int8>.new();
  v.push(4);
  v.push(3);
  v.push(1);
  bubblesort(v);
  return 0;
}
